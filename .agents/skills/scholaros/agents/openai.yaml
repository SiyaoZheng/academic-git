interface:
  display_name: "ScholarOS"
  short_description: "Repo-specific patterns and workflows for ScholarOS"
  default_prompt: "Use the ScholarOS repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true