interface:
  display_name: "Fu"
  short_description: "Repo-specific patterns and workflows for Fu"
  default_prompt: "Use the Fu repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true